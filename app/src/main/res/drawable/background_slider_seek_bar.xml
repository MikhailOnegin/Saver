<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/seek1" />
        </shape>
    </item>

    <item
        android:id="@android:id/progress"
        android:height="@dimen/seekBarHeight">
        <clip>
            <shape>
                <solid android:color="@color/seek3" />
            </shape>
        </clip>
    </item>
</layer-list>